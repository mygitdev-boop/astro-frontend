# Dependencies to add to your Flutter project's pubspec.yaml
# (see SETUP.md for exactly how to merge this in)

name: astro_bhavishya_ai
description: "Astro BhavishyaAI -- The next generation astrology app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cupertino_icons: ^1.0.6
  google_mobile_ads: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
